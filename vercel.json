{
  "installCommand": "pnpm install",
  "buildCommand": "pnpm --filter @workspace/miyar-capital run build && rm -rf public && cp -r artifacts/miyar-capital/dist/public public",
  "outputDirectory": "public",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
