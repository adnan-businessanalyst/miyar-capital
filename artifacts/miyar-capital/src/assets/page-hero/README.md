# Inner-page hero background

Default background for `PageHero` (all marketing inner pages).

**Path:** `artifacts/miyar-capital/src/assets/page-hero/page-hero-bg.png`

Replace this file to change the banner image site-wide. Prefer a wide still (~2400×1200, JPG/PNG/WebP). Keep the filename `page-hero-bg.png` (or update the import in `PageHero.tsx`).
