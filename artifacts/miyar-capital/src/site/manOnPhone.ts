/**
 * Shared "man on phone" image for CTA / contact sections.
 * Place man_on_phone.svg | .avif | .webp | .jpg | .jpeg | .png in
 * src/assets/content/
 */

import { CONTENT_IMAGES } from "./contentImages";

/** Resolved URL for the man-on-phone asset (empty if missing). */
export const MAN_ON_PHONE_IMG = CONTENT_IMAGES.man_on_phone;
