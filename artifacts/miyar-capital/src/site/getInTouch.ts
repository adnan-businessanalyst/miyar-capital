/**
 * Shared "get in touch" image for contact / register-interest modals.
 * Place get-in-touch.jpg (or .webp/.avif/…) in src/assets/content/
 * → public/media/content/
 */

import { CONTENT_IMAGES } from "./contentImages";

/** Resolved URL for the get-in-touch asset (empty if missing). */
export const GET_IN_TOUCH_IMG = CONTENT_IMAGES.get_in_touch;
