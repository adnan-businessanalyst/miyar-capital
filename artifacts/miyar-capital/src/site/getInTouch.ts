/**
 * Shared image for contact / register-interest modals.
 * Place contact-form.png (or .webp/.avif/…) in src/assets/content/
 * → public/media/content/
 */

import { CONTENT_IMAGES } from "./contentImages";

/** Resolved URL for the contact-form modal asset (empty if missing). */
export const GET_IN_TOUCH_IMG = CONTENT_IMAGES.contact_form;
